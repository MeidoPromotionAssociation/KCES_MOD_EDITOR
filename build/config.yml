# This file contains the configuration for this project.
# When you update `info` or `fileAssociations`, run `wails3 task common:update:build-assets` to update the assets.
# Note that this will overwrite any changes you have made to the assets.
version: '3'

# This information is used to generate the build assets.
info:
  companyName: "MEIDO Promotion Association" # The name of the company
  productName: "KCES MOD EDITOR" # The name of the application
  productIdentifier: "Github.MeidoPromotionAssociation.KCES_MOD_EDITOR" # The unique product identifier
  description: "All In One Modding tool for KCES" # The application description
  copyright: "(c) 2026, MeidoPromotionAssociation under BSD 3-Clause License" # Copyright text
  comments: "OHHH YEEEEEEEE" # Comments
  version: "0.0.0" # The application version
# Dev mode configuration
dev_mode:
  root_path: .
  log_level: warn
  debounce: 1000
  ignore:
    dir:
      - .git
      - node_modules
      - frontend
      - bin
    file:
      - .gitignore
      - .gitkeep
      - "*_test.go"
    watched_extension:
      - "*.go"
      - "*.js" # Watch for changes to JS/TS files included using the //wails:include directive.
      - "*.ts" # The frontend directory will be excluded entirely by the setting above.
    git_ignore: true
  executes:
    - cmd: wails3 build DEV=true
      type: blocking
    - cmd: wails3 task common:dev:frontend
      type: background
    - cmd: wails3 task run DEV=true
      type: primary

# File Associations
# More information at: https://v3.wails.io/noit/done/yet
fileAssociations:
  - ext: menuassets
    name: Menu Assets
    description: KCES Menu Bundle File
    iconName: menuassets
    role: Editor
  - ext: mateassets
    name: Mate Assets
    description: KCES Mate Bundle File
    iconName: mateassets
    role: Editor
  - ext: pmatassets
    name: PMat Assets
    description: KCES PMat Bundle File
    iconName: pmatassets
    role: Editor

# Other data
other:
  - name: My Other Data
